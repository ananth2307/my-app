import { get, isEmpty } from 'lodash'
import React, { memo } from 'react'

const CreateDeletedDetails = (props) => {
  // const createDeleteData = get(props,'bitBucketData.createDeleteDetailsData')
  // const {project,group,repository,user} = createDeleteData
  // const Headers = [' ',"Created","Deleted"]
  // let ProjectRow = [];
  // !isEmpty(project) && project.map((items)=>{

  // })
  return (
    <div>CreateDeletedDetails</div>
  )
}

export default memo(CreateDeletedDetails)